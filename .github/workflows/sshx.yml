name: 🚀 SSHX REDIRECT (HOSTED ON RDP REPO)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian chạy'
        default: '12h'
        type: choice
        options: ['1h', '3h', '6h', '12h']

jobs:
  Deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: 📥 CHECKOUT
        uses: actions/checkout@v4

      - name: 📥 SETUP SSHX
        run: |
          curl -sSf https://sshx.io/get | sh
          find $HOME/.sshx -name sshx -exec cp {} . \;
          chmod +x ./sshx

      - name: 🌐 RUN ROOT SSHX
        run: |
          sudo ./sshx > sshx_log.txt 2>&1 &
          sleep 12
          # Lấy Full URL có dấu #
          SSHX_URL=$(grep -o 'https://sshx.io/s/[^[:space:]]*' sshx_log.txt | head -n 1)
          echo "URL=$SSHX_URL" >> $GITHUB_ENV

      - name: ⚡ PUSH TO GH-PAGES (BRANCH RIÊNG)
        run: |
          # Tạo file redirect
          echo "<html><head><script>location.replace('${{ env.URL }}')</script></head></html>" > index.html
          
          # File CNAME định danh cho Domain riêng của bạn
          echo "ssh.nghuyzzz.qzz.io" > CNAME
          
          # Cấu hình Git
          git config --global user.name "ToiLaSkidder"
          git config --global user.email "ToiLaSkidder@users.noreply.github.com"
          
          # Đẩy lên nhánh gh-pages của repo RDP (không ảnh hưởng nhánh main)
          git checkout --orphan gh-pages
          git rm -rf .
          git add index.html CNAME
          git commit -m "🚀 Update Terminal Link"
          git push origin gh-pages --force
          
      - name: ⏳ KEEP ALIVE
        run: |
          case "${{ github.event.inputs.duration }}" in
            "1h") sleep 3600 ;;
            "3h") sleep 10800 ;;
            "6h") sleep 21600 ;;
            "12h") sleep 43200 ;;
          esac
