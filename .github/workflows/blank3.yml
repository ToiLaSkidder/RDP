name: 🚀 SEVER AI STV PREMIUM - FIXED 12H

on:
  workflow_dispatch:
    inputs:
      duration:
        description: '🕐 Thời gian sử dụng'
        required: false
        default: '12h'
        type: choice
        options:
        - '1h'
        - '3h' 
        - '6h'
        - '12h'

jobs:
  Premium-RDP-Setup:
    runs-on: windows-latest
    timeout-minutes: 720 
    
    steps:
      - name: 🎯 KHỞI ĐỘNG HỆ THỐNG
        run: |
          Write-Host "`n🤖 AI STV PREMIUM RDP SERVER" -ForegroundColor Yellow
          Write-Host "⚡ Powered by AI STV" -ForegroundColor Green
          Write-Host "🕒 Thời gian: ${{ github.event.inputs.duration }}" -ForegroundColor Magenta
          
      - name: 🔧 CẤU HÌNH HỆ THỐNG
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-Premium" dir=in action=allow protocol=TCP localport=3389 profile=any
          Start-Service -Name TermService -ErrorAction SilentlyContinue

      - name: 👤 TẠO TÀI KHOẢN NGHUYZZZ (FIXED PASS)
        run: |
          Write-Host "`n👤 ĐANG TẠO USER: nghuyzzz" -ForegroundColor Yellow
          # Mật khẩu mới có chữ Hoa và ký tự đặc biệt để tránh lỗi InvalidPasswordException
          $matKhau = "Huyngu67@" 
          $securePass = ConvertTo-SecureString $matKhau -AsPlainText -Force
          
          Remove-LocalUser -Name "nghuyzzz" -ErrorAction SilentlyContinue
          
          # Tạo user với mật khẩu chuẩn bảo mật
          New-LocalUser -Name "nghuyzzz" -Password $securePass -AccountNeverExpires -Description "AI STV Premium Admin"
          Set-LocalUser -Name "nghuyzzz" -PasswordNeverExpires $true
          
          Add-LocalGroupMember -Group "Administrators" -Member "nghuyzzz"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "nghuyzzz"
          
          Write-Host "│ ✅ User: nghuyzzz" -ForegroundColor Green
          Write-Host "│ ✅ Pass: Huyngu67@" -ForegroundColor Green

      - name: 🌐 THIẾT LẬP MẠNG TAILSCALE (FIXED PATH)
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          Write-Host "`n🌐 CÀI ĐẶT TAILSCALE..." -ForegroundColor Yellow
          $msiPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile $msiPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$msiPath`"", "/quiet", "/norestart" -Wait
          
          # Đợi dịch vụ khởi động và nạp lại biến môi trường
          Start-Sleep -Seconds 10
          $tailscaleExe = "C:\Program Files\Tailscale\tailscale.exe"
          
          if (Test-Path $tailscaleExe) {
              & $tailscaleExe up --authkey=$env:TAILSCALE_AUTH_KEY --hostname=ai-stv-nghuyzzz --reset
              $ip = & $tailscaleExe ip -4
              echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV
              Write-Host "│ ✅ IP: $ip" -ForegroundColor Green
          } else {
              Write-Host "│ ❌ Không tìm thấy file Tailscale tại đường dẫn cài đặt!" -ForegroundColor Red
              exit 1
          }

      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          $durationInput = "${{ github.event.inputs.duration }}"
          switch ($durationInput) {
              "1h" { $totalMinutes = 60 }
              "3h" { $totalMinutes = 180 }
              "6h" { $totalMinutes = 360 }
              "12h" { $totalMinutes = 720 }
          }
          $timeZone = [System.TimeZoneInfo]::FindSystemTimeZoneById("SE Asia Standard Time")
          $endTime = ([System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(), $timeZone)).AddMinutes($totalMinutes)
          
          Write-Host "`n┌───────────────────────────────────────────┐" -ForegroundColor Cyan
          Write-Host "│              🎉 KẾT NỐI SẴN SÀNG!                │" -ForegroundColor Cyan
          Write-Host "├───────────────────────────────────────────┤" -ForegroundColor Cyan
          Write-Host "│ 🌐  Địa chỉ: $env:TAILSCALE_IP" -ForegroundColor White
          Write-Host "│ 👤  Tài khoản: nghuyzzz" -ForegroundColor White
          Write-Host "│ 🔐  Mật khẩu: Huyngu67@" -ForegroundColor White
          Write-Host "│ ⏰  Kết thúc: $($endTime.ToString('HH:mm:ss dd/MM'))" -ForegroundColor Yellow
          Write-Host "└───────────────────────────────────────────┘" -ForegroundColor Cyan
          echo "TOTAL_MINUTES=$totalMinutes" >> $env:GITHUB_ENV

      - name: ⏳ DUY TRÌ PHIÊN LÀM VIỆC
        run: |
          $totalMinutes = $env:TOTAL_MINUTES
          $timeZone = [System.TimeZoneInfo]::FindSystemTimeZoneById("SE Asia Standard Time")
          $endTime = ([System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(), $timeZone)).AddMinutes($totalMinutes)
          
          # Chạy job ngầm canh dịch vụ
          Start-Job -ScriptBlock { while($true){ if((Get-Service TermService).Status -ne 'Running'){Start-Service TermService}; Start-Sleep 30 } }

          while ($true) {
              $now = [System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(), $timeZone)
              $left = $endTime - $now
              if ($left.TotalSeconds -le 0) { break }
              Write-Host ("`r⏳ Còn lại: {0:D2}h {1:D2}m {2:D2}s" -f $left.Hours, $left.Minutes, $left.Seconds) -NoNewline -ForegroundColor Cyan
              Start-Sleep -Seconds 1
          }
          Write-Host "`n🎯 ĐÃ HẾT THỜI GIAN!" -ForegroundColor Red

      - name: 🧹 DỌN DẸP
        if: always()
        run: |
          Get-Job | Stop-Job | Remove-Job
          $tailscaleExe = "C:\Program Files\Tailscale\tailscale.exe"
          if (Test-Path $tailscaleExe) { & $tailscaleExe logout }
